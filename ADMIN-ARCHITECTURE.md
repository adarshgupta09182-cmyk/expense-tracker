# Admin Dashboard - Architecture & System Design

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER BROWSER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │           ADMIN DASHBOARD (Netlify)                      │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  Frontend Pages                                    │  │   │
│  │  │  ├── index.html (Registration)                    │  │   │
│  │  │  ├── login.html (Login)                           │  │   │
│  │  │  └── dashboard.html (Main Dashboard)              │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                            │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  Frontend Logic (JavaScript)                       │  │   │
│  │  │  ├── register.js                                  │  │   │
│  │  │  ├── login.js                                     │  │   │
│  │  │  └── dashboard.js                                 │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                            │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  Styling (CSS)                                     │  │   │
│  │  │  ├── style.css                                    │  │   │
│  │  │  └── dashboard.css                                │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                            │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  Local Storage                                     │  │   │
│  │  │  ├── adminToken (JWT)                             │  │   │
│  │  │  └── apiUrl (Backend URL)                         │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              │ REST API
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND API (Railway)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Express.js Server (server.js)                           │   │
│  │  ├── Authentication Endpoints                           │   │
│  │  │   ├── POST /api/admin/register                       │   │
│  │  │   └── POST /api/admin/login                          │   │
│  │  │                                                       │   │
│  │  ├── User Management Endpoints                          │   │
│  │  │   ├── GET /api/admin/users                           │   │
│  │  │   ├── GET /api/admin/users/:userId                   │   │
│  │  │   └── DELETE /api/admin/users/:userId                │   │
│  │  │                                                       │   │
│  │  ├── Expense Management Endpoints                       │   │
│  │  │   └── DELETE /api/admin/expenses/:expenseId          │   │
│  │  │                                                       │   │
│  │  └── Statistics Endpoints                               │   │
│  │      └── GET /api/admin/stats                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Middleware                                              │   │
│  │  ├── authenticate (JWT validation)                      │   │
│  │  ├── authorize (role-based access)                      │   │
│  │  ├── validate (input validation)                        │   │
│  │  └── CORS (cross-origin protection)                     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Security                                                │   │
│  │  ├── bcrypt (password hashing)                          │   │
│  │  ├── JWT (token generation)                             │   │
│  │  ├── helmet (security headers)                          │   │
│  │  └── rate limiting                                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ File I/O
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATA STORAGE (JSON Files)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  users.json                                              │   │
│  │  [                                                       │   │
│  │    {                                                     │   │
│  │      "id": 123,                                          │   │
│  │      "name": "Admin Name",                               │   │
│  │      "email": "admin@example.com",                       │   │
│  │      "password": "hashed_password",                      │   │
│  │      "role": "admin",                                    │   │
│  │      "createdAt": "2024-01-15T10:00:00Z"                │   │
│  │    },                                                    │   │
│  │    ...                                                   │   │
│  │  ]                                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  expenses.json                                           │   │
│  │  [                                                       │   │
│  │    {                                                     │   │
│  │      "id": 456,                                          │   │
│  │      "userId": 123,                                      │   │
│  │      "description": "Groceries",                         │   │
│  │      "amount": 500,                                      │   │
│  │      "category": "Food",                                 │   │
│  │      "date": "2024-01-15"                                │   │
│  │    },                                                    │   │
│  │    ...                                                   │   │
│  │  ]                                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Admin Registration Flow
```
User Input (Registration Form)
    ↓
register.js (Client-side validation)
    ↓
POST /api/admin/register (with admin secret)
    ↓
server.js (Validate admin secret)
    ↓
bcrypt.hash (Hash password)
    ↓
users.json (Save admin)
    ↓
Response: Success/Error
    ↓
Redirect to Login
```

### Admin Login Flow
```
User Input (Login Form)
    ↓
login.js (Client-side validation)
    ↓
POST /api/admin/login
    ↓
server.js (Find user by email)
    ↓
bcrypt.compare (Verify password)
    ↓
generateToken (Create JWT)
    ↓
Response: Token + User Info
    ↓
localStorage.setItem('adminToken', token)
    ↓
Redirect to Dashboard
```

### Dashboard Data Flow
```
Dashboard Load
    ↓
Check adminToken in localStorage
    ↓
If no token → Redirect to Login
    ↓
If token exists → Load Dashboard
    ↓
Tab Click (Users/Expenses/Stats)
    ↓
Fetch Data with Authorization Header
    ↓
server.js (Verify JWT)
    ↓
server.js (Verify Admin Role)
    ↓
Read from JSON files
    ↓
Process Data (Calculate stats, filter, etc.)
    ↓
Return JSON Response
    ↓
dashboard.js (Render UI)
    ↓
Display Data to User
```

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  1. REGISTRATION                                             │
│     ├── User enters: name, email, password, admin secret    │
│     ├── Client validates input                              │
│     ├── POST /api/admin/register                            │
│     ├── Server validates admin secret                       │
│     ├── Server hashes password with bcrypt                  │
│     ├── Server saves to users.json                          │
│     └── Response: Success/Error                             │
│                                                               │
│  2. LOGIN                                                    │
│     ├── User enters: email, password                        │
│     ├── Client validates input                              │
│     ├── POST /api/admin/login                               │
│     ├── Server finds user by email                          │
│     ├── Server compares password with bcrypt                │
│     ├── Server generates JWT token                          │
│     ├── Response: Token + User Info                         │
│     └── Client stores token in localStorage                 │
│                                                               │
│  3. AUTHENTICATED REQUESTS                                  │
│     ├── Client includes: Authorization: Bearer <token>      │
│     ├── Server middleware: authenticate()                   │
│     ├── Server verifies JWT signature                       │
│     ├── Server extracts user ID from token                  │
│     ├── Server middleware: authorize('admin')               │
│     ├── Server checks user role is 'admin'                  │
│     ├── If valid → Process request                          │
│     └── If invalid → Return 401 Unauthorized                │
│                                                               │
│  4. LOGOUT                                                   │
│     ├── User clicks logout button                           │
│     ├── Client removes token from localStorage              │
│     ├── Client redirects to login page                      │
│     └── Session ends                                        │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Component Interaction

```
┌──────────────────────────────────────────────────────────────┐
│                    COMPONENT DIAGRAM                          │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  index.html (Registration Page)                         │ │
│  │  ├── Form Input                                         │ │
│  │  ├── Validation Messages                                │ │
│  │  └── register.js (Event Listeners)                      │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  login.html (Login Page)                                │ │
│  │  ├── Form Input                                         │ │
│  │  ├── Validation Messages                                │ │
│  │  └── login.js (Event Listeners)                         │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  dashboard.html (Main Dashboard)                        │ │
│  │  ├── Sidebar Navigation                                 │ │
│  │  │   ├── Users Tab Button                               │ │
│  │  │   ├── Expenses Tab Button                            │ │
│  │  │   ├── Statistics Tab Button                          │ │
│  │  │   └── Logout Button                                  │ │
│  │  │                                                       │ │
│  │  ├── Header                                             │ │
│  │  │   ├── Page Title                                     │ │
│  │  │   ├── Search Box                                     │ │
│  │  │   └── Admin Name                                     │ │
│  │  │                                                       │ │
│  │  ├── Users Tab Content                                  │ │
│  │  │   ├── User Cards (loadUsers)                         │ │
│  │  │   └── User Modal (openUserModal)                     │ │
│  │  │                                                       │ │
│  │  ├── Expenses Tab Content                               │ │
│  │  │   └── Expense Cards (loadExpenses)                   │ │
│  │  │                                                       │ │
│  │  ├── Statistics Tab Content                             │ │
│  │  │   └── Stat Cards (loadStats)                         │ │
│  │  │                                                       │ │
│  │  └── dashboard.js (All Logic)                           │ │
│  │      ├── switchTab()                                    │ │
│  │      ├── loadUsers()                                    │ │
│  │      ├── loadExpenses()                                 │ │
│  │      ├── loadStats()                                    │ │
│  │      ├── openUserModal()                                │ │
│  │      ├── deleteUser()                                   │ │
│  │      ├── deleteExpense()                                │ │
│  │      └── Search functionality                           │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                                │
└──────────────────────────────────────────────────────────────┘
```

## 🔌 API Integration

```
┌──────────────────────────────────────────────────────────────┐
│                    API INTEGRATION POINTS                     │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  REGISTRATION PAGE                                            │
│  └── register.js                                              │
│      └── POST /api/admin/register                             │
│          ├── Request: { name, email, password, adminSecret }  │
│          └── Response: { success, message }                   │
│                                                                │
│  LOGIN PAGE                                                   │
│  └── login.js                                                 │
│      └── POST /api/admin/login                                │
│          ├── Request: { email, password }                     │
│          └── Response: { token, user }                        │
│                                                                │
│  DASHBOARD - USERS TAB                                        │
│  └── dashboard.js                                             │
│      ├── GET /api/admin/users                                 │
│      │   └── Response: { data: [users] }                      │
│      ├── GET /api/admin/users/:userId                         │
│      │   └── Response: { data: { user, expenses } }           │
│      └── DELETE /api/admin/users/:userId                      │
│          └── Response: { success, message }                   │
│                                                                │
│  DASHBOARD - EXPENSES TAB                                     │
│  └── dashboard.js                                             │
│      └── DELETE /api/admin/expenses/:expenseId                │
│          └── Response: { success, message }                   │
│                                                                │
│  DASHBOARD - STATISTICS TAB                                   │
│  └── dashboard.js                                             │
│      └── GET /api/admin/stats                                 │
│          └── Response: { data: { stats } }                    │
│                                                                │
└──────────────────────────────────────────────────────────────┘
```

## 🎯 Request/Response Cycle

```
CLIENT                          SERVER                          DATABASE
  │                               │                               │
  ├─ User Action ─────────────────>│                               │
  │                                │                               │
  │                                ├─ Validate Input ─────────────>│
  │                                │                               │
  │                                │<─ Read Data ─────────────────┤
  │                                │                               │
  │                                ├─ Process Data                 │
  │                                │                               │
  │                                ├─ Write Data ─────────────────>│
  │                                │                               │
  │<─ JSON Response ───────────────┤                               │
  │                                │                               │
  ├─ Parse Response                │                               │
  │                                │                               │
  ├─ Update UI                     │                               │
  │                                │                               │
  └─ Display to User               │                               │
```

## 📈 Scalability Considerations

### Current Architecture
- JSON file storage (suitable for small to medium deployments)
- Single admin portal instance
- Stateless API (can scale horizontally)

### Future Improvements
- Migrate to MongoDB for better scalability
- Add caching layer (Redis)
- Implement pagination for large datasets
- Add audit logging
- Implement rate limiting per user
- Add data export functionality

## 🔒 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Layer 1: HTTPS/TLS                                          │
│  └── All communication encrypted                             │
│                                                               │
│  Layer 2: CORS                                               │
│  └── Only allowed origins can access API                     │
│                                                               │
│  Layer 3: Input Validation                                   │
│  └── All inputs validated before processing                  │
│                                                               │
│  Layer 4: Authentication                                     │
│  └── JWT tokens required for protected endpoints             │
│                                                               │
│  Layer 5: Authorization                                      │
│  └── Role-based access control (admin only)                  │
│                                                               │
│  Layer 6: Password Security                                  │
│  └── bcrypt hashing with salt >= 12                          │
│                                                               │
│  Layer 7: Rate Limiting                                      │
│  └── Prevent brute force attacks                             │
│                                                               │
│  Layer 8: Security Headers                                   │
│  └── Helmet.js for additional security                       │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Data Model

```
USERS TABLE (users.json)
├── id (number)
├── name (string)
├── email (string)
├── password (string - hashed)
├── role (string - 'admin' or 'user')
└── createdAt (ISO string)

EXPENSES TABLE (expenses.json)
├── id (number)
├── userId (number - foreign key)
├── description (string)
├── amount (number)
├── category (string)
└── date (ISO string)
```

---

**Architecture Version**: 1.0.0
**Last Updated**: February 2024
**Status**: Production Ready
